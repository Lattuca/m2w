<%= form_for(@user_access_level) do |f| %>
  <% if @user_access_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_access_level.errors.count, "error") %> prohibited this user_access_level from being saved:</h2>

      <ul>
      <% @user_access_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :access_level %><br>
    <%= f.text_field :access_level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
